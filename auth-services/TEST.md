# testing here the route for TTD - 

setup test with the help of jest for /auth/register , use mongodb memory database/server to don't flood the original db

We'll need to install Jest and MongoDB Memory Server:

```sh
npm install --save-dev jest @babel/preset-env babel-jest mongodb-memory-server
```
